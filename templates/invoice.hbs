<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Invoice {{invoice_no}}</title>
  <style>
    @page { size: A4; margin: 10mm; }
    body { font-family: Arial, Helvetica, sans-serif; color: #111; font-size: 12px; }
    .header { margin-bottom: 16px; }
    .title { font-size: 18px; font-weight: bold; margin-bottom: 6px; }
    .meta { line-height: 1.4; }
    .section-title { font-weight: bold; margin: 16px 0 8px; font-size: 13px; }
    .grid { width: 100%; border-collapse: collapse; }
    .grid td, .grid th { border: 1px solid #ddd; padding: 6px; vertical-align: top; }
    .grid th { background: #f5f5f5; }
    .right { text-align: right; }
    .muted { color: #555; }
    ul { margin: 8px 0; padding-left: 18px; }
  </style>
  </head>
<body>
  <div class="header">
    <div class="title">ZRS CARS TRADING – INVOICE</div>
    <div class="meta">Invoice No: <strong>{{invoice_no}}</strong></div>
    <div class="meta">Date: <strong>{{date}}</strong></div>
    <div class="meta">Investor Name: <strong>{{investor_name}}</strong></div>
    <div class="meta">Prepared By: <strong>{{prepared_by}}</strong></div>
    <div class="meta">Reference PO No: <strong>{{reference_po_no}}</strong></div>
  </div>

  <div class="section-title">Vehicle Details</div>
  <table class="grid">
    <tr><td>Car Make</td><td><strong>{{car_make}}</strong></td></tr>
    <tr><td>Car Model</td><td><strong>{{car_model}}</strong></td></tr>
    <tr><td>Trim</td><td><strong>{{trim}}</strong></td></tr>
    <tr><td>Year Model</td><td><strong>{{year_model}}</strong></td></tr>
    <tr><td>Chassis No.</td><td><strong>{{chassis_no}}</strong></td></tr>
  </table>

  <div class="section-title">Invoice Summary</div>
  <table class="grid">
    <tr><td>Buying Price</td><td class="right">AED {{buying_price}}</td></tr>
    <tr><td>Transfer Cost (RTA)</td><td class="right">AED {{transfer_cost}}</td></tr>
    <tr><td>Detailing / Inspection Cost</td><td class="right">AED {{detailing_inspection_cost}}</td></tr>
    <tr><td>Other Charges (if any)</td><td class="right">AED {{other_charges}}</td></tr>
    <tr><th>Total Amount Payable</th><th class="right">AED {{total_amount_payable}}</th></tr>
  </table>

  <div class="section-title">Payment Details</div>
  <table class="grid">
    <tr><td>Mode of Payment</td><td>{{mode_of_payment}}</td></tr>
    <tr><td>Payment Received By</td><td>{{payment_received_by}}</td></tr>
    <tr><td>Date of Payment</td><td>{{date_of_payment}}</td></tr>
  </table>

  <div class="section-title">Notes</div>
  <ul class="muted">
    <li>This invoice is issued against the signed Purchase Order No. {{reference_po_no}}.</li>
    <li>Payment confirms investor participation in the vehicle investment under ZRS Cars Trading.</li>
    <li>All amounts received are recorded under the investor’s account and will be settled upon vehicle sale as per the Investment Agreement.</li>
    <li>Any additional costs (inspection, recovery, or others) will be adjusted from the investor’s final proceeds.</li>
  </ul>

  <p>For ZRS Cars Trading</p>
  <p>Authorized Signatory<br/>ZRS Cars Trading – DIP 1, Dubai</p>
</body>
</html>


